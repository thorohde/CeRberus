% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remove_PCs.R
\name{remove_PCs}
\alias{remove_PCs}
\title{Remove selected principal components from a data matrix}
\usage{
remove_PCs(.x, to_remove = NA, .center = TRUE, .scale = TRUE)
}
\arguments{
\item{.x}{Numeric matrix-like object with observations in rows and variables
in columns. Passed to \code{\link[stats:prcomp]{stats::prcomp()}}.}

\item{to_remove}{Integer vector of principal component indices to remove. Use
\code{NA} to skip PC removal.}

\item{.center}{Logical scalar passed to the \code{center} argument of
\code{\link[stats:prcomp]{stats::prcomp()}}. If \code{TRUE}, variables are centered before PCA and the
center is added back after reconstruction.}

\item{.scale}{Logical scalar passed to the \code{scale.} argument of
\code{\link[stats:prcomp]{stats::prcomp()}}. If \code{TRUE}, variables are scaled before PCA and the scale
is applied back after reconstruction.}
}
\value{
A numeric matrix with the same dimensions as \code{.x}, reconstructed after
setting the selected principal component scores to zero. If \code{to_remove} is
\code{NA}, returns \code{.x} unchanged.
}
\description{
Performs principal component analysis on a numeric matrix-like object, sets
selected principal component scores to zero, and reconstructs the data in the
original variable space. This can be used to remove dominant axes of variation
such as batch effects or other unwanted structure captured by specific PCs.

If \code{to_remove} is \code{NA}, no PCs are removed and \code{.x} is returned unchanged.
}
\examples{
x <- matrix(rnorm(50), nrow = 10)
remove_PCs(x, to_remove = 1)
remove_PCs(x, to_remove = NA)

}
