% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor_GIScores.R
\name{GIScores}
\alias{GIScores}
\title{Construct a genetic-interaction screen object}
\usage{
GIScores(
  input,
  query_col = "query_gene",
  lib_col = "library_gene",
  bio_rep_col = "bio_rep",
  tech_rep_col = "tech_rep",
  guide_col = "guide_pair",
  gi_col = "GI",
  collapse_layers = NULL,
  block_layer = NULL,
  force_fixed_pair = FALSE,
  pos_agnostic = FALSE,
  symmetric_analysis_method = "preaverage",
  verbose = FALSE,
  screen_type = c("auto", "fixed_pair", "multiplex")
)
}
\arguments{
\item{input}{A data frame or data.table containing guide-level GI scores.}

\item{query_col}{Name of the column containing query-gene identifiers.}

\item{lib_col}{Name of the column containing library-gene identifiers.}

\item{bio_rep_col}{Name of the biological-replicate column.}

\item{tech_rep_col}{Name of the technical-replicate column.}

\item{guide_col}{Name of the guide-pair column.}

\item{gi_col}{Name of the numeric genetic-interaction score column.}

\item{collapse_layers}{Optional character vector of replicate layers to
average before model fitting, for example \code{"guide_pair"}, \code{"tech_rep"}, or
\code{"bio_rep"}.}

\item{block_layer}{Optional replicate layer used as the limma blocking layer
for duplicate-correlation modelling.}

\item{force_fixed_pair}{Deprecated logical override. Use
\code{screen_type = "fixed_pair"} instead.}

\item{pos_agnostic}{Logical. If \code{TRUE}, opt into position-agnostic analysis
for an inferred multiplex screen by averaging both orientations of each
gene pair. The default, \code{FALSE}, retains directional query-by-library
analysis.}

\item{symmetric_analysis_method}{Character scalar specifying how
position-agnostic multiplex screens are modeled. \code{preaverage} averages
both pair orientations before fitting one limma model per query gene.
\code{global_preaverage} performs the same orientation averaging, converts the
result to one row per unordered gene pair, and fits one limma model across
all unordered pairs. This argument is only used when
\code{pos_agnostic = TRUE}; directional analysis remains the default.}

\item{verbose}{Logical. If \code{TRUE}, print a short screen summary.}

\item{screen_type}{Character scalar controlling screen classification.
\code{"auto"} uses the inferred screen attributes, while \code{"fixed_pair"} and
\code{"multiplex"} explicitly select the corresponding screen structure.}
}
\value{
An S4 object inheriting from \code{ScreenBase}, typically a
\code{FixedPairScreen}, \code{MultiplexScreen}, or \code{PosAgnMultiplexScreen}.
}
\description{
\code{GIScores()} imports guide-level genetic-interaction scores, infers the
screen structure, stores the scores in CeRberus S4 containers, and prepares
the object for duplicate-correlation estimation and limma-based aggregation.

The input table is expected in long format with one guide-pair observation
per row. Column names can be customized with the \verb{*_col} arguments and are
standardized internally.
}
